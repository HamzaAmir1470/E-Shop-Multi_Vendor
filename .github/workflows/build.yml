name: Docker MERN CI Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Docker layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      # New Step: Generates the directory and file expected by docker-compose
      - name: Create Mock Environment Configuration
        run: |
          mkdir -p BackEnd/config
          touch BackEnd/config/.env

      - name: Build Docker Services
        env:
          AWS_ACCOUNT_ID: "123456789012"
          AWS_REGION: "us-east-1"
        run: |
          docker compose build

      - name: Start Services
        env:
          AWS_ACCOUNT_ID: "123456789012"
          AWS_REGION: "us-east-1"
        run: |
          docker compose up -d

      - name: Verify Running Containers
        run: |
          docker ps
          
      - name: Run Tests (Optional)
        run: |
          echo "Services built and verified successfully!"

      - name: Stop Services
        if: always()
        env:
          AWS_ACCOUNT_ID: "123456789012"
          AWS_REGION: "us-east-1"
        run: |
          docker compose down